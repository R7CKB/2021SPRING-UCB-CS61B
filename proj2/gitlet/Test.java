package gitlet;

import java.io.File;
import java.io.Serializable;
import java.text.SimpleDateFormat;
import java.util.Date;
import java.util.List;
import java.util.Locale;
import java.util.TimeZone;

import static gitlet.Utils.*;

public class Test implements Serializable {

    public static void main(String[] args) {
//        File CWD = new File(System.getProperty("user.dir"));
//        File GITLET_DIR = join(CWD, ".gitlet");
//        File MASTER_DIR = join(GITLET_DIR, "refs/heads/master");
//        File otherDir = join(GITLET_DIR, "refs/heads/other");
//        System.out.println(GITLET_DIR.getName());
//        MASTER_DIR.mkdirs();
//        otherDir.mkdirs();
//        savefile();
//        Commit currentCommit = readObject(new File(Commit.COMMITS_DIR,
//                readContentsAsString(Repository.MASTER_BRANCH_FILE)), Commit.class);
//        List<String> branchList = plainFilenamesIn(join(Repository.GITLET_DIR, "refs", "heads"));
//        for (String branch : branchList) {
//            System.out.println(branch);
//        }
//    }

//        private static void savefile() {
////        // serialize and deserialize a file
////        File file = new File("1.txt");
////        String content = readContentsAsString(file);
////        String id = sha1(content);
////        Dumpable blob = new Blob("1.txt", id, content);
////        Dumpable blob2;
////        File testFile2 = new File("2.txt");
////        writeObject(testFile2, blob);
////        blob2 = readObject(testFile2, Dumpable.class);
////        blob2.dump();
//
//        }
    }
}